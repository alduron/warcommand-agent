bin/
obj/
publish/
*.user
*.suo
.vs/

# Speech models are large and downloaded at install time, never committed.
models/

# Captured frames must never be committed except the curated golden set.
!WarCommand.Agent.Tests/Capture/frames/
*.png
!WarCommand.Agent.Tests/Capture/frames/*.png

*.log
TestResults/

# Local dev TLS proxy: each developer generates their own cert and key. See DEVELOPING.md.
dev/cert.pem
dev/key.pem
