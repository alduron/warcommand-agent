<Window x:Class="WarCommand.Agent.Overlay.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WarCommand.Agent.Overlay"
        Title="WarCommand Overlay"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="{x:Null}"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        Focusable="False"
        IsHitTestVisible="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">
    <!-- No chrome, no ground, no hit testing. The four extended styles that make this a real
         in-game surface are applied in OnSourceInitialized; XAML cannot express them.

         The window is the whole anchor rect and the board aligns inside it, rather than the window
         sizing to the board. A bottom-anchored panel has to sit ON the bottom edge, and a window
         that shrinks to its content cannot: it would float by however many rows are missing. -->
    <Grid>
        <!-- Two star rows rather than an alignment: the anchor nudge is continuous, so the board
             has to be able to sit anywhere between the two edges rather than at one of three. -->
        <Grid.RowDefinitions>
            <RowDefinition x:Name="LeadRow" Height="1*" />
            <RowDefinition Height="Auto" />
            <RowDefinition x:Name="TrailRow" Height="1*" />
        </Grid.RowDefinitions>
        <local:BoardView x:Name="Board" Grid.Row="1" />
    </Grid>
</Window>
